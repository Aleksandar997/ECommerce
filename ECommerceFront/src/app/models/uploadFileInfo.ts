export class UploadFileInfo {
    currentName: string;
    pastName: string;

    constructor(currentName: string, pastName: string) {
        this.currentName = currentName;
        this.pastName = pastName;
    }
}
